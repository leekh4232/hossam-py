{
  "cline": {
    "mcpServers": [
      {
        "name": "hossam",
        "command": "python",
        "args": ["-m", "hossam.mcp.server"],
        "env": {
          "PYTHONPATH": "${workspaceFolder}"
        },
        "disabled": false
      }
    ]
  }
}
